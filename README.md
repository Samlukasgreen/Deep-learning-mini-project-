To run the Filmnet training, you need to run the HDF5 creation first 
To run the HDF5 training you also need to download the dataset from harvard: https://dataverse.harvard.edu/dataset.xhtml?persistentId=doi:10.7910/DVN/VJT9EP
it will take a minute but will create a file that you then need to run the training. 
Run the tranining, using v2, creating the model

Then run the pointnet stuff
